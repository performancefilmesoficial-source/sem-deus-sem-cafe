import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.antigrafity.planobiblico',
  appName: 'Plano Bíblico',
  webDir: 'dist'
};

export default config;
